#!/usr/bin/perl -i
# Perl script that Jochen wrote for me to create .smt2 scripts for the 
# SMT competition
#
# In the bash shell you can apply this script to all files in the folder using the
# for i in *.smt2 ; do perl addSetInfoLassoRanker.pl $i; done

while (<>) {
  print $_;
  if (/^\(set-logic.*\)/) {
     print '(set-info :source |
SMT script generated by the tool Ultimate LassoRanker.
    http://ultimate.informatik.uni-freiburg.de/LassoRanker/
This script encodes the existence of a certain termination 
argument (a certain ranking function together with supporting invariants of a
certain form) for a given lasso program.
|)
(set-info :smt-lib-version 2.0)
(set-info :category "industrial")
';

  }
}

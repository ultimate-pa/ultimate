(set-option :produce-proofs true)
(set-option :proof-check-mode true)
(set-option :interactive-mode true)

(set-logic QF_AUFLIA)
(declare-sort U 0)
(declare-fun i () U)
(declare-fun j () U)
(declare-fun k () U)
(declare-fun v () U)
(declare-fun f (U) U)
(declare-fun a () (Array U U))
(declare-fun b () (Array U U))
(assert (= i j))
(assert (not (= i k)))
(assert (not (= (f i) k)))
(assert (= b (store a k v)))
(assert (not (= (select a (f i)) (select b (f j)))))

(check-sat)
(set-option :print-terms-cse false)
(get-proof)
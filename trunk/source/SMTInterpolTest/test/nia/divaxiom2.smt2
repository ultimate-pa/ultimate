(set-logic QF_UFNIA)

(declare-const x Int)
(declare-const d Int)
(declare-const a Int)
(declare-const b Int)
(push 1)
(assert (>= (mod x d) (abs d)))
(assert (not (= d 0)))
(check-sat)
(pop 1)

(push 1)
(assert (>= x (+ (abs d) (* d (div x d)))))
(assert (not (= d 0)))
(check-sat)
(pop 1)


(push 1)
(assert (>= x (+ (- d) (* d (div x d)))))
(assert (< d 0))
(check-sat)
(pop 1)
(push 1)
(assert (>= x (+ d (* d (div x d)))))
(assert (> d 0))
(check-sat)
(pop 1)

(push 1)
(assert (>= x (+ 5 (* 5 (div x 5)))))
(check-sat)
(pop 1)
(push 1)
(assert (>= x (+ 5 (* (- 5) (div x (- 5))))))
(check-sat)
(pop 1)

(push 1)
(assert (>= x (+ (- (- a b)) (* (- a b) (div x (- a b))))))
(assert (< (- a b) 0))
(check-sat)
(get-proof)
(pop 1)

(push 1)
(assert (>= x (+ (- a b) (* (- a b) (div x (- a b))))))
(assert (> (- a b) 0))
(check-sat)
(get-proof)
(pop 1)

(push 1)
(assert (>= x (+ (* (- 5) d) (* (* (- 5) d) (div x (* (- 5) d))))))
(assert (> (* (- 5) d) 0))
(check-sat)
(get-proof)
(pop 1)



/* This file was generated by SableCC (http://www.sablecc.org/). */

package de.uni_freiburg.informatik.ultimate.automata.parser.TestGrammar.node;

import java.util.*;
import de.uni_freiburg.informatik.ultimate.automata.parser.TestGrammar.analysis.*;

@SuppressWarnings("nls")
public final class ANetTransition extends PNetTransition
{
    private final LinkedList<PIdentifier> _predecessors_ = new LinkedList<PIdentifier>();
    private PIdentifier _symbol_;
    private final LinkedList<PIdentifier> _successors_ = new LinkedList<PIdentifier>();

    public ANetTransition()
    {
        // Constructor
    }

    public ANetTransition(
        @SuppressWarnings("hiding") List<PIdentifier> _predecessors_,
        @SuppressWarnings("hiding") PIdentifier _symbol_,
        @SuppressWarnings("hiding") List<PIdentifier> _successors_)
    {
        // Constructor
        setPredecessors(_predecessors_);

        setSymbol(_symbol_);

        setSuccessors(_successors_);

    }

    @Override
    public Object clone()
    {
        return new ANetTransition(
            cloneList(this._predecessors_),
            cloneNode(this._symbol_),
            cloneList(this._successors_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseANetTransition(this);
    }

    public LinkedList<PIdentifier> getPredecessors()
    {
        return this._predecessors_;
    }

    public void setPredecessors(List<PIdentifier> list)
    {
        this._predecessors_.clear();
        this._predecessors_.addAll(list);
        for(PIdentifier e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    public PIdentifier getSymbol()
    {
        return this._symbol_;
    }

    public void setSymbol(PIdentifier node)
    {
        if(this._symbol_ != null)
        {
            this._symbol_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._symbol_ = node;
    }

    public LinkedList<PIdentifier> getSuccessors()
    {
        return this._successors_;
    }

    public void setSuccessors(List<PIdentifier> list)
    {
        this._successors_.clear();
        this._successors_.addAll(list);
        for(PIdentifier e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._predecessors_)
            + toString(this._symbol_)
            + toString(this._successors_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._predecessors_.remove(child))
        {
            return;
        }

        if(this._symbol_ == child)
        {
            this._symbol_ = null;
            return;
        }

        if(this._successors_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PIdentifier> i = this._predecessors_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PIdentifier) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        if(this._symbol_ == oldChild)
        {
            setSymbol((PIdentifier) newChild);
            return;
        }

        for(ListIterator<PIdentifier> i = this._successors_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PIdentifier) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}

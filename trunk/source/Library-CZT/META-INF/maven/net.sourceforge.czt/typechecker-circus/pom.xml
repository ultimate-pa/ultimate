<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>net.sourceforge.czt</groupId>
    <artifactId>czt</artifactId>
    <version>1.5-SNAPSHOT</version>
  </parent>
  <artifactId>typechecker-circus</artifactId>
  <packaging>jar</packaging>
  <name>CZT Circus Typechecker</name>
  <!-- 
    changed IDE editor settings to use UTF-8, rather than windows-1252.
    this was creating an error within the javac "unmappable character to UTF-8".
  -->     
  <description>Provides classes for typechecking Circus specifications.</description>
  <!--
  <build>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>RELEASE</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>RELEASE</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>    
  <profiles>
        <profile>
            <id>netbeans-public</id>
            <activation>
                <property>
                    <name>netbeans.execution</name>
                    <value>true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <artifactId>maven-jar-plugin</artifactId>
                        <configuration>
                            <archive>
                                <manifest>
                                    <addClasspath>true</addClasspath>
                                    <classpathPrefix>lib</classpathPrefix>
                                    <mainClass>net.sourceforge.czt.typecheck.circus.TypeCheckUtils</mainClass>
                                </manifest>
                            </archive>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>nb</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>directory</goal>
                                </goals>
                                <configuration>
                                    <descriptor>${basedir}/src/main/assemblies/netbeans-run.xml</descriptor>
                                    <finalName>executable</finalName>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>                           
                </plugins>
            </build>
        </profile>
    </profiles>
    -->
  <dependencies>
     <dependency>
      <groupId>${czt.groupId}</groupId>
      <artifactId>corejava</artifactId>      
    </dependency>
    <dependency>
      <groupId>${czt.groupId}</groupId>
      <artifactId>parser-circus</artifactId>      
    </dependency>    
    <dependency>
      <groupId>${czt.groupId}</groupId>
      <artifactId>typechecker</artifactId>            
    </dependency> 
    <dependency>
      <groupId>${czt.groupId}</groupId>
      <artifactId>typechecker</artifactId>
      <type>test-jar</type>
    </dependency> 
  </dependencies>  
  <!--
  <properties>
        <netbeans.hint.useExternalMaven>true</netbeans.hint.useExternalMaven>
    </properties>  
    -->
</project>

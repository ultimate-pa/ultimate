// Author: schaetzc@tf.uni-freiburg.de
// Date:  2018-07-12
//
// Net without accepting places.

assert(isEmpty(n));
assert(numberOfConditions(finitePrefix(n)) == 3);

PetriNet n = (
  alphabet = {a},
  places = {p},
  transitions = {
    ({p} a {p})
  },
  initialMarking = {p},
  acceptingPlaces = {}
);

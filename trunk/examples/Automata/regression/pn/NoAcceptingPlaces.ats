// Author: schaetzc@tf.uni-freiburg.de
// Date:  2018-07-12
//
// Net without accepting places.

assert(isEmpty(noAccepting));
assert(numberOfConditions(finitePrefix(noAccepting)) == 2);

PetriNet noAccepting = (
  alphabet = {a},
  places = {p},
  transitions = {
    ({p} a {p})
  },
  initialMarking = {p},
  acceptingPlaces = {}
);

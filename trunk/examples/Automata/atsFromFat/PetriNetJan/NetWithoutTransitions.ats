// 14.01.2010
// heizmann@informatik.uni-freiburg.de
//
// Bug: In revision r3061 the getInitialConditions() method of OccurenceNet
// returns an empty Collection.

print(finitePrefix(net));

PetriNet net = (
  alphabet = {a b c},
  places = {s1 s2 s3 s4},
  transitions = {
		   },
  initialMarking = {s1 s2},
  #acceptingMarkings = {{s1 s2}}
);

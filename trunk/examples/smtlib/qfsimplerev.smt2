(set-option :produce-proofs true)
(set-info :source "{ Simple test for interpolation with quantified formulae }")
(set-logic AUFLIA)
(declare-fun P ( Int) Bool)
(declare-fun a () Int)
(assert (! (not (P a)) :named IP_0))
(assert (! (forall ((x Int)) (P x)) :named IP_1))
(check-sat)
(get-interpolants IP_0 IP_1)
(exit)

(set-logic QF_UFLIA)
(declare-fun x () Int)
(declare-fun y () Int)
(declare-fun f (Int) Int)
(push)
(assert (= (f x) (f y)))
(check-sat)
(assert (distinct (f (+ x 5)) (f (+ y 5))))
(check-sat)
(assert (= x y))
(check-sat)
(pop)
(push)
(assert (= (f (+ x 5)) 42))
(check-sat)
(assert (> (f x) (f y)))
(check-sat)
(assert (= (f x) (f (+ x 5))))
(check-sat)
(assert (= y (+ x 5)))
(check-sat)
(exit)

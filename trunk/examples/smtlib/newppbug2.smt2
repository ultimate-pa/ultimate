(set-logic QF_UFLIA)
(declare-fun x () Int)
(declare-fun y () Int)
(declare-fun f (Int) Int)
(assert (= (f x) (f y)))
(check-sat)
(push)
(assert (= (f (+ x 5)) (f y)))
(check-sat)
(pop)
(check-sat)
(assert (distinct y (+ x 5)))
(check-sat)
(assert (and (< y (+ x 6)) (> y (+ x 4))))
(check-sat)
(assert (distinct (f y) (f (+ x 5))))
(check-sat)
(exit)

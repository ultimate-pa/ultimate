format_version: '2.0'

input_files: 'disjoint.bpl'

properties:
  - property_file: ../properties/unreach-call.prp
    expected_verdict: true

options:
  language: BPL
  de.uni_freiburg.informatik.ultimate.plugins.generator.viewabstraction:
    Minimum view abstraction level: 3
    Maximum view abstraction level: 3
    Enable persistent set reduction: true


#include <stdio.h>
#include <math.h>

int main() {
  double x = -0.0;
  double y = 1.0;
  double n = NAN;
  double n2 = nanl("");
  double n3 = nanl("");
  double inf = INFINITY;
  if (n2 == n2) {
    if (n2 == n3) {
      return 0;
    }
  } 
  if (x == y)
  {
    return 1;
  } else {
  return 0;
  }
}

procedure main()
{
   var x, y, z : int;
   var b : bool;
   
   assume y % 4 == 0;
   assume x % 3 == y;
   
   // b := x % 5 == -1;
   // assume b;
   // assume !((x + 2) * (3 + 7) + (14 % -5) * (8 / 7) * y == 0 && y == 7);
}
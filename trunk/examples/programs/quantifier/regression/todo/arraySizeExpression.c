/*
 * Date: November 2013
 * Author: Christian Schilling
 * 
 * Array has expression as size definition
 * and is initialized directly.
 * The size must be computed, but this is tricky.
 * Furthermore, the initialization is only given
 * partially.
 */
int main() {
    int arr[10 * 10] = { 1 };
    
    return 0;
}
